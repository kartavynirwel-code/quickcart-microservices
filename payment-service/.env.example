# Copy to .env (or set as real env vars / k8s Secret+ConfigMap) and fill in.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=quickcart
DB_USER=root
DB_PASSWORD=password

# TODO: update once your OTel collector / Tempo instance is deployed
OTEL_EXPORTER_OTLP_ENDPOINT=http://tempo.tracing.svc.cluster.local:4318/v1/traces
