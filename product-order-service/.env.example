# Copy to .env (or set as real env vars / k8s Secret+ConfigMap) and fill in.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=quickcart
DB_USER=root
DB_PASSWORD=password

# Hostname of payment-service. Use the k8s service name once deployed, e.g. http://payment-service:8000
PAYMENT_SERVICE_URL=http://localhost:8000
